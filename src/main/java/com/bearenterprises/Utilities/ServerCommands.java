package com.bearenterprises.Utilities;

public enum ServerCommands {
   REGISTER_USER,
   LOGIN_USER,
   GET_CREDENTIALS,
   RIGHT_CREDENTIALS,
   WRONG_CREDENTIALS,
   USER_ALREADY_EXIST,
   USER_REGISTERED,
   EXCHANGE_KEYS,
   KILL_WORKER
}
